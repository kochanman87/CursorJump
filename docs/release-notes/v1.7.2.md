## v1.7.2

### 不具合対策・安定性向上
- カーソル移動時のエフェクト（軌跡・収縮円）が突然表示されなくなる症状の自己修復処理を追加しました。再起動なしで復旧します。
- エフェクト描画中に内部で発生していた例外を `debug.log` に記録するようにしました（症状の原因解析用）。
- 自動起動が動作しないケースの原因解析用に、起動時の判定結果と exe パスを `debug.log` に記録するようにしました。
