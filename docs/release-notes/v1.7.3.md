## v1.7.3 (pre-release)

### 診断ログ追加（メモリ成長問題の原因特定用）
- 起動時および 1 分毎にメモリ使用状況（WorkingSet / Private / GC.Total）と軌跡オーバーレイの Canvas 子要素数を `debug.log` に記録するようにしました。長時間使用でメモリが大きく成長する症状の原因調査用です（次バージョンで対策予定）。

### 内部変更
- 機能や操作感に変更はありません。
